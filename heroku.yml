build:
  languages:
    - python
    - go
  config:
    python_version: 3.9
    go_version: 1.18
  packages:
    - ./oprfservice
  
run:
  web: gunicorn wsgi:app
  oprf: bin/oprfservice --port=${PORT} --keydir=./instance/data/keys

stack: heroku-22