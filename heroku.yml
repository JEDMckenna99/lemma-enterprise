build:
  languages:
    - python
    - go
  config:
    python_version: 3.9
  pre:
    - chmod +x bin/heroku-build.sh
    - bin/heroku-build.sh
  
run:
  web: gunicorn wsgi:app
  oprf: ./bin/oprfservice --port=${PORT} --keydir=./instance/data/keys